# OpenCode Memory Plugin v2.2-v3.0 实施路线图

**版本**：Final v1.0（经专家评审）  
**日期**：2026-03-18  
**状态**：待审核  
**评审专家**：架构师、性能专家、安全专家、UX专家、项目经理

---

## 执行摘要

本路线图提出了 OpenCode Memory Plugin 从 v2.2-lite 到 v3.0 的渐进式升级方案，核心目标是实现**分层存储架构**和**完整的双模式同步机制**，最终达到：

- **Token 消耗降低 95%**（从 20,000 → 1,000 tokens/启动）
- **搜索速度提升 10 倍**（从 1000ms → 100ms）
- **本地存储优化**（自动压缩归档）
- **同步可靠性 99%+**（增量 + 全量双模式）

**实施策略**：三阶段渐进式实施，每个阶段独立验证、可回滚，总工作量 **30.5-34.5 小时**（约 4-5 周）。

**关键改进**（基于专家评审）：
- ✅ 增强安全性（API 认证、敏感信息检测、加密存储）
- ✅ 后端数据库优化（向量搜索性能提升50%、传输加密）
- ✅ 调整工作量估算（+50%，包含测试、调试和后端优化）
- ✅ 优化 Phase B（主题预分析、灰度迁移、一致性检查）
- ✅ 改进用户体验（进度可视化、冲突解决 UI、主题管理工具）

**专家评审总评分**：⭐⭐⭐⭐☆ (3.9/5)

---

## Phase A（v2.2-lite）- 基础分层 + 安全增强

**目标**：验证核心概念，快速获得 80% 收益，建立安全基础

**架构决策**：重建而非迁移 - 旧记忆（MEMORY.md.backup）作为只读档案，新记忆使用新格式重建

**工作量**：4-4.5 小时（编码 3-3.5h + 测试 1h）

### 核心任务

| 任务 | 工作量 | 优先级 | 说明 |
|------|--------|--------|------|
| 1.1 目录结构创建 | 10min | P0 | CORE_DIR, TIMELINE_DIR, SYNC_DIR |
| 1.2 核心函数实现 | 45min | P0 | writeToTimeline, updateDayOverview, updateMemoryIndex |
| 1.3 memory_write 改造 | 30min | P0 | 添加 abstract/overview 必填参数 |
| 1.4 getMemoryFiles 适配 | 20min | P0 | 返回新目录结构，包含 MEMORY.md.backup |
| 1.5 简单增量同步 | 30min | P0 | checkpoint.jsonl + mtime/hash 检查 |
| **1.6 安全增强（新增）** | **1h** | **P0** | **API 认证 + 敏感信息检测 + 加密** |
| 1.7 初始化脚本 | 10min | P0 | 重置 MEMORY.md（用户已手动备份） |
| **1.8 后端数据库优化（新增）** | **2-3h** | **P0** | **修复性能和安全问题（专家评审）** |
| 1.9 验证和测试 | 1h | P0 | 7 个 Go/No-Go 检查点 |

### 安全增强详解（新增任务 1.6）

```javascript
// 1. API 认证配置
const client = new SecureClient(API_KEY, TENANT_ID);

// 2. 敏感信息检测（6 种模式）
function detectSensitiveInfo(text) {
  const patterns = [
    /password[:\s]*[^\s]+/i,
    /api[_-]?key[:\s]*[^\s]+/i,
    /token[:\s]*[^\s]+/i,
    /bearer\s+[^\s]+/i,
    /[a-f0-9]{32,}/i,  // 可能的哈希或密钥
    /sk-[a-zA-Z0-9]{32,}/  // OpenAI API Key 格式
  ];
  
  for (const pattern of patterns) {
    if (pattern.test(text)) {
      return { detected: true, type: pattern.source };
    }
  }
  return { detected: false };
}

// 3. checkpoint 加密存储
function saveCheckpointSecure(data) {
  const encrypted = encrypt(JSON.stringify(data), getEncryptionKey());
  fs.writeFileSync(CHECKPOINT_PATH, encrypted);
}

// 在 memory_write 中使用
async execute({ content, abstract, overview, type, tags }) {
  // 敏感信息检测
  const sensitiveCheck = detectSensitiveInfo(content);
  if (sensitiveCheck.detected) {
    return `❌ 检测到敏感信息：${sensitiveCheck.type}\n请移除后重试`;
  }
  
  // ... 其他逻辑
}
```

### 后端数据库优化详解（新增任务 1.8）

**背景**：专家评审发现后端存在性能和安全问题，需要立即修复。

**P0 优化（立即修复）**：

```python
# 1. 消除重复向量计算（性能提升50%）
# 当前实现（问题）
q = """SELECT id, vector::similarity::cosine(embedding, $e) AS score
       FROM memory WHERE vector::similarity::cosine(embedding, $e) >= 0.95"""

# 优化方案
q = """SELECT * FROM memory 
       WHERE tenant_id = $tenant_id
       AND embedding <|10,COSINE|> $query_embedding"""

# 2. 启用传输加密
# 当前：ws://localhost:18002
# 优化：wss://localhost:18002（配置TLS证书）
```

**P1 优化（强烈建议）**：

```python
# 3. 智能去重决策框架
def decide_duplicate_action(new_mem, old_mem, similarity):
    time_diff = (new_mem['created_at'] - old_mem['created_at']).total_seconds()
    if similarity > 0.98 and time_diff < 300: return 'DISCARD'
    if new_mem['type'] == 'preference': return 'UPDATE'
    if new_mem['type'] == 'decision': return 'KEEP_BOTH'
    if len(new_mem['content']) > len(old_mem['content']) * 1.5: return 'UPDATE'
    return 'KEEP_BOTH'

# 4. 动态去重阈值
THRESHOLDS = {'preference': 0.88, 'decision': 0.90, 'general': 0.95}

# 5. 批量插入事务
q = "INSERT INTO memory $entries"  # 数组插入，10-20x提升

# 6. 查询结果缓存（TTL 5分钟）
cache = TTLCache(maxsize=500, ttl=300)
```

**预期效果**：
- 向量搜索性能提升50%
- 支持智能去重（UPDATE/DISCARD/KEEP_BOTH）
- 批量操作性能提升10-20x
- 传输安全合规

### Go/No-Go 检查点（8 个）

1. ✅ 目录结构创建成功（CORE_DIR, TIMELINE_DIR, SYNC_DIR 存在）
2. ✅ timeline 写入成功（entry-*.md 文件创建）
3. ✅ MEMORY.md ≤200 行（从 3254 行降低）
4. ✅ memory_write 接收 abstract/overview 参数（必填验证）
5. ✅ 后端同步正常（memory_id 正确返回）
6. ✅ 增量同步只上传变更文件（mtime + hash 检查生效）
7. ✅ **敏感信息检测工作正常（新增，准确率 > 95%）**
8. ✅ **后端数据库优化完成（P0: 向量搜索性能提升50%，传输加密启用）**

### 完成标志

- ✅ MEMORY.md 从 82KB → <10KB
- ✅ Token 消耗降低 60%（从 20,000 → 8,000 tokens）
- ✅ 同步延迟降低 50%（从 2000ms → 1000ms）
- ✅ **API 认证启用，所有请求携带 Bearer Token**
- ✅ **敏感信息检测启用，拒绝包含密码/密钥的内容**
- ✅ **向量搜索延迟降低50%（消除重复计算，使用KNN搜索）**
- ✅ **传输加密启用（ws:// → wss://）**

### 回滚方案

```bash
# scripts/rollback-phase-a.sh
# 恢复备份（用户已手动备份到 memory.backup）
rm -rf ~/.opencode/memory/
cp -r ~/.opencode/memory.backup ~/.opencode/memory/
# 恢复旧的 plugin.js
git checkout HEAD~1 -- opencode-memory-plugin/plugin.js
```

---

*（文档第一部分完成，继续追加 Phase B...）*

## Phase B（v2.3）- 主题组织 + 完整双模式同步

**目标**：实现按主题组织 + 完整的双模式同步机制

**工作量**：12-15 小时（编码 8-10h + 测试 2-3h + 调整 2h）

### 核心任务

| 任务 | 工作量 | 优先级 | 说明 |
|------|--------|--------|------|
| **2.0 主题预分析（新增）** | **1h** | **P0** | **生成主题分布报告，用户确认** |
| 2.1 目录结构重组 | 1.5h | P0 | active/{topic}/ 结构 |
| 2.2 完整双模式同步 | 3h | P0 | 增量 + 全量 + 冲突处理 |
| 2.3 URL 引用机制 | 1h | P1 | {directory}/{file}#{anchor} |
| 2.4 link-map.json 实现 | 1h | P0 | ID → 路径映射 |
| 2.5 MEMORY.md 新格式 | 30min | P0 | 索引的索引（≤10KB） |
| **2.6 一致性检查工具（新增）** | **30min** | **P1** | **自动检测和修复不一致** |
| **2.7 UX 改进（新增）** | **1h** | **P1** | **进度条 + 主题管理 + 冲突解决** |
| 2.8 验证和测试 | 2-3h | P0 | 10 个 Go/No-Go 检查点 |

### 新增任务详解

**2.0 主题预分析**（1h）：
```javascript
async function analyzeTopics() {
  const entries = await scanAllEntries();
  const topicDistribution = new Map();
  
  for (const entry of entries) {
    const topic = resolveTopicFromEntry(entry);
    topicDistribution.set(topic, (topicDistribution.get(topic) || 0) + 1);
  }
  
  console.log('📊 主题分布预览：');
  for (const [topic, count] of topicDistribution) {
    console.log(`  ${topic}: ${count} 条目`);
  }
  
  return topicDistribution;
}
```

**2.6 一致性检查工具**（30min）：
```javascript
async function checkConsistency() {
  const linkMap = loadLinkMap();
  const issues = [];
  
  for (const [entryId, metadata] of Object.entries(linkMap.entries)) {
    const filePath = metadata.l2_path;
    
    if (!fs.existsSync(filePath)) {
      issues.push({ type: 'missing_file', entryId, path: filePath });
    } else {
      const currentHash = computeHash(filePath);
      if (currentHash !== metadata.content_hash) {
        issues.push({ type: 'hash_mismatch', entryId, path: filePath });
      }
    }
  }
  
  return issues;
}
```

**2.7 UX 改进**（1h）：
- 迁移进度条：实时显示进度和预计剩余时间
- 主题管理 CLI：`opencode-memory topic list/move/merge`
- 冲突解决 UI：交互式选择本地/远程/合并

### 灰度迁移策略

1. **Phase 1**：迁移 decisions 主题（验证）
2. **Phase 2**：迁移 preferences 主题
3. **Phase 3**：迁移 patterns 和 lessons 主题
4. **Phase 4**：迁移 general 主题

每个阶段完成后验证无误，再继续下一个阶段。

### Go/No-Go 检查点（10 个）

1. ✅ 主题预分析报告生成
2. ✅ 主题识别准确率 > 80%
3. ✅ active/ 目录结构正确
4. ✅ link-map.json 有效
5. ✅ MEMORY.md ≤10KB
6. ✅ 增量同步工作正常
7. ✅ 全量同步工作正常
8. ✅ 冲突处理机制工作
9. ✅ 一致性检查通过
10. ✅ **用户反馈积极（新增）**

### 完成标志

- ✅ Token 消耗降低 85%（从 20,000 → 3,000 tokens）
- ✅ 同步可靠性 > 99%
- ✅ 主题组织清晰
- ✅ **用户满意度 > 4/5**

---


## Phase C（v3.0）- 本地索引 + 高级特性

**目标**：实现本地加速索引 + 后端分层存储 + 自动归档

**工作量**：11.5-13 小时（编码 8.5-10h + 测试 3h）

### 核心任务

| 任务 | 工作量 | 优先级 | 说明 |
|------|--------|--------|------|
| 3.1 本地 Trie 索引 | 2h | P0 | 关键词快速匹配（<10ms） |
| 3.2 后端向量索引优化 | 2h | P1 | 分层索引 + 批量处理 + 缓存 |
| 3.3 后端分层存储 | 3h | P1 | 4 表设计 + API 改造 |
| 3.4 自动归档机制 | 2h | P1 | 90 天后压缩归档到本地 |
| 3.5 实时同步 | 1.5-2h | P2 | 文件监听 + 自动增量同步 |
| **3.6 性能优化（新增）** | **1h** | **P1** | **压缩 Trie + 动态批次** |
| 3.7 验证和测试 | 3h | P0 | 10 个 Go/No-Go 检查点 |

### 性能优化详解（新增任务 3.6）

**压缩 Trie 索引**：
- 使用 Patricia Trie 替代标准 Trie
- 内存占用减少 50%
- 10000 个条目：从 10MB → 5MB

**后端向量索引优化**（任务 3.2）：
- 分层向量索引：L0（abstract）、L1（overview）、L2（content）
- 批量embedding处理：减少API调用次数
- 结果缓存：常见查询缓存30分钟
- 索引预热：启动时加载热点数据

**动态批次大小**：
- 根据网络状况自动调整
- 快速网络：batchSize = 100
- 慢速网络：batchSize = 20

### Go/No-Go 检查点（10 个）

1. ✅ Trie 索引搜索 <10ms
2. ✅ 后端向量索引优化完成
3. ✅ 混合搜索 <100ms
4. ✅ 后端分层存储正常
5. ✅ 归档策略工作
6. ✅ 实时同步连接
7. ✅ **索引大小 <50MB（新增）**
8. ✅ **内存占用 <100MB（新增）**
9. ✅ **离线回退功能正常**
10. ✅ MEMORY.md 仍然 ≤10KB

### 完成标志

- ✅ Token 消耗降低 95%（从 20,000 → 1,000 tokens）
- ✅ 搜索速度提升 10 倍（从 1000ms → 100ms）
- ✅ 本地存储优化（自动压缩归档）
- ✅ **性能指标全部达标**
- ✅ **用户满意度 > 4.5/5**

---


## 关键变更说明

### 相比原方案的 5 大改进

**1. 安全增强（响应安全专家评审 ⭐⭐⭐☆☆）**

| 变更项 | 原方案 | 改进后 | 影响 |
|--------|--------|--------|------|
| API 认证 | ❌ 无 | ✅ Bearer Token | 防止未授权访问 |
| 敏感信息检测 | ❌ 无 | ✅ 6 种模式检测 | 防止泄露密码/密钥 |
| checkpoint 加密 | ❌ 明文 | ✅ AES-256 | 保护本地数据 |
| 归档压缩 | ❌ 无 | ✅ .tar.gz 压缩 | 节省本地存储空间 |

**实施优先级**：P0（必须在 Phase A 完成）

**2. 工作量调整（响应项目经理评审 ⭐⭐⭐⭐☆）**

| 阶段 | 原估算 | 调整后 | 增加原因 |
|------|--------|--------|---------|
| Phase A | 2-3h | 6.5-7h | +1.5h 安全增强 + 2.5h 后端优化 + 测试 |
| Phase B | 8-10h | 12-15h | +2-3h 测试 + 2h 主题调整 + 1h UX |
| Phase C | 10-12h | 11.5-13h | +3h 测试 + 1h 性能优化（简化边缘计算） |
| **总计** | **20-25h** | **30.5-34.5h** | **+50%（包含测试、调试和后端优化）** |

**实际周期**：4-5 周（而非原计划的 2-3 周）

**3. Phase B 优化（响应架构师评审 ⭐⭐⭐⭐⭐）**

新增任务：
- ✅ **主题预分析**（1h）：生成报告，用户确认后再迁移
- ✅ **灰度迁移**：逐个主题迁移，降低风险
- ✅ **一致性检查工具**（30min）：自动检测和修复不一致

**收益**：
- 主题识别准确率从 60-70% 提升到 80%+
- 迁移失败风险降低 50%
- 数据一致性保证 99%+

**4. UX 改进（响应 UX 专家评审 ⭐⭐⭐⭐☆）**

新增功能：
- ✅ **迁移进度条**：实时显示进度和预计剩余时间
- ✅ **主题管理 CLI**：`topic list/move/merge`
- ✅ **冲突解决 UI**：交互式选择本地/远程/合并

**收益**：
- 用户满意度预计提升 30%
- 减少 80% 的"迁移卡住"问题反馈
- 主题管理效率提升 5 倍

**5. 性能优化（响应性能专家评审 ⭐⭐⭐⭐☆）**

新增优化：
- ✅ **压缩 Trie**：内存占用减少 50%
- ✅ **向量索引分片**：按主题分片，按需加载
- ✅ **动态批次大小**：根据网络状况自动调整
- ✅ **性能监控**：记录和报告性能指标

**收益**：
- 大规模数据（10000+ 条目）时仍能保持性能
- 启动时间从 500ms 降到 100ms
- 内存占用从 100MB 降到 50MB

---

## 风险评估和缓解措施

### 高风险（P0）

**风险 1：Phase B 主题识别准确率低（<80%）**
- **影响**：大量条目被错误分类，需要手动调整
- **概率**：中（30-40%）
- **缓解措施**：
  1. Phase A 完成后，先运行主题预分析
  2. 生成报告供用户确认
  3. 提供主题管理工具快速调整
  4. 如果准确率 <70%，回退到 Phase A
- **应急方案**：保留 timeline/ 目录作为备份

**风险 2：迁移过程中数据丢失**
- **影响**：用户数据永久丢失
- **概率**：低（5-10%）
- **缓解措施**：
  1. 迁移前自动备份 MEMORY.md
  2. 迁移过程中保留原文件
  3. 验证通过后才删除原文件
  4. 提供回滚脚本
- **应急方案**：从备份恢复

**风险 3：安全漏洞被利用**
- **影响**：敏感信息泄露、未授权访问
- **概率**：中（20-30%）
- **缓解措施**：
  1. Phase A 必须完成安全增强
  2. 代码审查（安全专家）
  3. 渗透测试
  4. 定期安全更新
- **应急方案**：紧急安全补丁

### 中风险（P1）

**风险 4：性能不达标**
- **影响**：用户体验差，搜索慢
- **概率**：中（20-30%）
- **缓解措施**：每个阶段都有性能基准测试，不达标时优化或降级
- **应急方案**：降级到 Phase B

**风险 5：工作量超出预期**
- **影响**：延期交付
- **概率**：高（40-50%）
- **缓解措施**：已调整工作量估算（+40%），每个阶段设置缓冲时间
- **应急方案**：分阶段交付

**风险 6：用户接受度低**
- **影响**：用户不愿升级
- **概率**：低（10-20%）
- **缓解措施**：提供详细的用户指南，录制视频教程，渐进式升级
- **应急方案**：保持向后兼容

---


## 成功标准

### Phase A 成功标准

**技术指标**：
- ✅ MEMORY.md 大小 <10KB（从 82KB）
- ✅ Token 消耗降低 60%（从 20,000 → 8,000 tokens）
- ✅ 同步延迟降低 50%（从 2000ms → 1000ms）
- ✅ 所有 7 个 Go/No-Go 检查点通过
- ✅ 敏感信息检测准确率 > 95%

**用户体验**：
- ✅ 迁移时间 <30 分钟
- ✅ 无数据丢失
- ✅ 用户可正常使用 memory_write

**项目管理**：
- ✅ 实际工作量 ≤4 小时
- ✅ 无 P0/P1 bug
- ✅ 文档完整

### Phase B 成功标准

**技术指标**：
- ✅ Token 消耗降低 85%（从 20,000 → 3,000 tokens）
- ✅ 同步可靠性 > 99%
- ✅ 主题识别准确率 > 80%
- ✅ 所有 10 个 Go/No-Go 检查点通过

**用户体验**：
- ✅ 迁移进度可视化
- ✅ 主题管理工具可用
- ✅ 冲突解决 UI 友好
- ✅ 用户满意度 > 4/5

**项目管理**：
- ✅ 实际工作量 ≤15 小时
- ✅ 7 天内无 P0/P1 bug

### Phase C 成功标准

**技术指标**：
- ✅ Token 消耗降低 95%（从 20,000 → 1,000 tokens）
- ✅ 搜索速度 <100ms（从 1000ms）
- ✅ 索引大小 <50MB
- ✅ 内存占用 <100MB
- ✅ 所有 10 个 Go/No-Go 检查点通过

**用户体验**：
- ✅ 搜索体验显著提升
- ✅ 实时同步工作正常
- ✅ 用户满意度 > 4.5/5

**项目管理**：
- ✅ 实际工作量 ≤15 小时
- ✅ 14 天内无 P0/P1 bug

### 整体成功标准

- ✅ 三个阶段全部完成
- ✅ 性能指标全部达标
- ✅ 用户满意度 > 4.5/5
- ✅ 总工作量 ≤34 小时
- ✅ 交付周期 ≤5 周

---

## 下一步行动

### 立即行动（本周）

**1. 审核和确认**（1-2 天）
- [ ] 审核本路线图文档
- [ ] 确认工作量估算和时间表
- [ ] 确认安全要求和优先级
- [ ] 决定是否采纳所有改进建议

**2. 环境准备**（1 天）
- [ ] 备份当前 MEMORY.md（82KB）
- [ ] 准备测试环境
- [ ] 配置 API Key 和认证
- [ ] 安装必要的依赖

**3. Phase A 启动**（3-4 小时）
- [ ] 实施核心功能（2-3h）
- [ ] 实施安全增强（30min）
- [ ] 运行验证脚本（1h）
- [ ] 确认所有检查点通过

### 短期计划（1-2 周）

- [ ] Phase A 稳定运行
- [ ] 监控性能指标
- [ ] 收集用户反馈
- [ ] Phase B 准备（主题预分析）

### 中期计划（3-4 周）

- [ ] Phase B 实施（12-15h）
- [ ] 灰度迁移
- [ ] Phase B 稳定运行

### 长期计划（5-6 周）

- [ ] Phase C 实施（13-15h）
- [ ] 性能基准测试
- [ ] 用户满意度调查

---

## 总结

本路线图提出了一个**渐进式、低风险、高收益**的升级方案，通过三个阶段逐步实现分层存储架构和完整的双模式同步机制。

**核心价值**：
- 🎯 Token 节省 95%
- ⚡ 搜索提速 10 倍
- 💰 成本降低 70%
- 🔒 安全增强

**关键决策**（基于专家评审）：
1. ✅ 采用渐进式实施
2. ✅ 增强安全性（Phase A 必须完成）
3. ✅ 调整工作量估算（+40%）
4. ✅ 优化 Phase B（主题预分析 + 灰度迁移）
5. ✅ 改进用户体验

**预期成果**：
- 4-5 周内完成全部三个阶段
- 用户满意度 > 4.5/5
- 性能指标全部达标

**专家评审总评分**：⭐⭐⭐⭐☆ (3.9/5)

---

**文档版本**：Final v1.0  
**最后更新**：2026-03-18  
**状态**：✅ 待审核

