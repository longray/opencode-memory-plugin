# 代码分析功能 Backlogs

> **文档类型**: Backlog  
> **对应计划**: [4 周探索计划](./PLAN-4-WEEK-EXPLORATION.md)  
> **创建时间**: 2026-04-04  
> **状态**: 活跃

---

## 使用说明

本文档记录 4 周探索计划中的所有 Backlog 项。

每个 Backlog 项包含：

- **目标**: 要达成什么
- **涉及范围**: 影响哪些文件/模块
- **前置依赖**: 开始前必须完成什么
- **完成标准**: 怎样算"完成"
- **验证方式**: 如何验证"完成"

---

## Week 1 Backlogs

### BL-W1-001: 依赖关系审计

**目标**: 彻底识别所有依赖问题，形成修复清单

**涉及范围**:

- `opencode-memory-plugin/package.json`
- 所有 `node_modules` 中的实际使用依赖
- 开发和生产环境的依赖声明

**前置依赖**: 无

**完成标准**:

1. 列出所有 `dependencies` 和 `devDependencies`
2. 识别运行时必需但在 `devDependencies` 中的包（特别是 `oxc-parser`）
3. 识别声明了但未使用的依赖（如 `tree-sitter-*`）
4. 识别版本不匹配的依赖
5. 形成按严重性排序的依赖问题列表

**验证方式**:

- **检查清单**: 对照完成标准的 5 项，确认全部完成
- **审查输出**: 依赖问题列表必须有明确的修复建议和优先级

---

### BL-W1-002: 代码结构审计

**目标**: 理解代码架构，识别结构问题和与文档的差异

**涉及范围**:

- `opencode-memory-plugin/lib/code-analyzer.js`
- `opencode-memory-plugin/lib/code-analysis-service.js`
- `opencode-memory-plugin/lib/code-fingerprint.js`
- `opencode-memory-plugin/lib/privacy-filter.js`
- `opencode-memory-plugin/cli/code-analyzer.cjs`
- `opencode-memory-plugin/plugin.js`

**前置依赖**: 无

**完成标准**:

1. 绘制模块依赖图（哪些文件依赖哪些文件）
2. 识别循环依赖（如果有）
3. 识别未使用的代码（dead code）
4. 识别硬编码的配置（应该可配置但没有）
5. 对照设计文档 v1.2，列出哪些功能已实现、哪些声称实现但实际没有

**验证方式**:

- **检查清单**: 对照完成标准的 5 项，确认全部完成
- **审查输出**: 代码结构审计报告必须包含模块依赖图和与文档的差异分析

---

### BL-W1-003: 测试状态审计

**目标**: 评估测试覆盖和可运行性

**涉及范围**:

- `opencode-memory-plugin/code-analyzer/*.test.ts`（7 个测试文件）
- `opencode-memory-plugin/package.json` 中的 `test` 脚本
- Jest 配置文件

**前置依赖**: 无

**完成标准**:

1. 列出所有测试文件
2. 标记每个测试使用的框架（Bun vs Jest）
3. 尝试运行测试，记录哪些能运行、哪些失败、原因是什么
4. 估算修复测试的工作量（语法迁移、配置调整等）
5. 给出测试优先级建议（哪些测试最关键）

**验证方式**:

- **检查清单**: 对照完成标准的 5 项，确认全部完成
- **审查输出**: 测试状态审计报告必须包含每个测试文件的运行状态和修复工作量估算

---

### BL-W1-004: 功能实现审计

**目标**: 对比设计文档 v1.2，评估功能完整度

**涉及范围**:

- `CODE-ANALYSIS-DESIGN-v1.2.md` 中承诺的所有功能
- 实际代码实现状态

**前置依赖**: 无

**完成标准**:

1. 列出设计文档 v1.2 承诺的所有功能
2. 标记每个功能的实现状态：
   - ✅ 已实现（功能完整可用）
   - ⚠️ 部分实现（功能可用但不完整）
   - ❌ 未实现（功能不存在）
   - 🚫 虚假声称（文档说有但实际没有）
3. 估算实现缺失功能的工作量
4. 给出功能优先级建议（哪些功能最有价值）

**验证方式**:

- **检查清单**: 对照完成标准的 4 项，确认全部完成
- **审查输出**: 功能实现审计报告必须包含每个功能的状态标记和工作量估算

---

## Week 2 Backlogs（基于 Week 1 审计结果制定）

**注意**：Week 2 的具体 Backlog 项将在 Week 1 审计完成后，根据审计结果制定。

### 可能的 Backlog 项（示例，以实际审计结果为准）

#### BL-W2-001: oxc-parser 依赖修复

**目标**: 将 `oxc-parser` 从 `devDependencies` 移到 `dependencies`
**涉及范围**: `package.json`
**前置依赖**: Week 1 审计确认此问题是关键阻塞点
**完成标准**: `npm install` 后 `oxc-parser` 被正确安装到 `node_modules`
**验证方式**: 运行 `node -e "require('oxc-parser')"`，无错误

#### BL-W2-002: CLI 入口注册

**目标**: 在 `package.json` 的 `bin` 字段注册 `code-analyze` 命令
**涉及范围**: `package.json`
**前置依赖**: Week 1 审计确认此问题是关键阻塞点
**完成标准**: 安装后能运行 `opencode-memory code-analyze test.js`
**验证方式**: 运行 `opencode-memory code-analyze --help`，看到帮助信息

#### BL-W2-003: 最小测试集修复

**目标**: 修复 5-10 个核心测试用例，让测试能运行
**涉及范围**: `code-analyzer/*.test.ts`
**前置依赖**: Week 1 审计确认测试状态
**完成标准**: `npm test` 运行，5-10 个核心测试通过
**验证方式**: `npm test`，查看通过/失败数量

**其他可能的 Backlog 项**:

- 代码重构（移除 dead code、修复硬编码配置等）
- 错误处理完善（添加有意义的错误信息）
- 基础文档编写（关键决策记录）

---

## Week 3 Backlogs（基于 Week 2 修复结果和使用场景制定）

**注意**：Week 3 的具体 Backlog 项将在 Week 2 修复完成并确定使用场景后制定。

### 可能的场景（示例）

#### 场景 A: 手动触发麻烦

**痛点**: "每次都要手动运行命令，太麻烦了，我想保存文件后自动分析"
**可能的 Backlog 项**:

- BL-W3-A001: 调研文件监听方案（`fs.watch` vs `chokidar`）
- BL-W3-A002: 实现文件监听模块
- BL-W3-A003: 集成防抖逻辑（300ms）
- BL-W3-A004: 测试自动触发流程

#### 场景 B: 想分析其他语言

**痛点**: "我想分析 Python/Go/Rust/Java 代码，但只能用 JS/TS"
**可能的 Backlog 项**:

- BL-W3-B001: 调研 Tree-sitter WASM 最新版本
- BL-W3-B002: 实现 Tree-sitter WASM 加载器
- BL-W3-B003: 实现 Python 符号提取器
- BL-W3-B004: 实现 Go 符号提取器
- ...

#### 场景 C: 分析结果不够详细

**痛点**: "分析结果太简单了，我想要更详细的符号信息"
**可能的 Backlog 项**:

- BL-W3-C001: 完善 Oxc 符号提取（箭头函数、变量等）
- BL-W3-C002: 优化复杂度计算算法
- BL-W3-C003: 添加 JSDoc 注释提取

---

## Week 4 Backlogs（基于 Week 3 使用验证结果制定）

**注意**：Week 4 的具体 Backlog 项将在 Week 3 使用验证完成后，基于验证结果和决策制定。

### 可能的决策（示例）

#### 决策 A: 继续完善（验证成功）

**情况**: 痛点可以被解决，价值被验证，愿意继续投入
**可能的 Backlog 项**:

- BL-W4-A001: 完善测试覆盖
- BL-W4-A002: 性能优化
- BL-W4-A003: 错误处理完善
- BL-W4-A004: 文档完善

#### 决策 B: 调整方向（验证发现新方向）

**情况**: 发现更大的痛点需要优先解决，或发现这个功能不如预期有价值
**可能的 Backlog 项**:

- BL-W4-B001: 重新评估假设
- BL-W4-B002: 制定新的实施计划
- BL-W4-B003: 原型验证新方向

#### 决策 C: 暂停/结束（验证失败）

**情况**: 痛点太多或无法解决，价值未被验证
**可能的 Backlog 项**:

- BL-W4-C001: 归档项目
- BL-W4-C002: 记录经验教训
- BL-W4-C003: 考虑其他方向

---

## 使用说明

### 如何阅读本文档

1. **Week 1 是确定的**：前 4 个 Backlog 项（依赖审计、代码结构审计、测试状态审计、功能实现审计）是确定的，应该立即开始执行。

2. **Week 2+ 是动态的**：Week 2、3、4 的 Backlog 项是"可能的示例"，实际 Backlog 项应该基于前一阶段的输出制定。

3. **优先级标记**：
   - 🔴 **最高优先级**：阻塞性问题，必须解决
   - 🟡 **中优先级**：重要但不阻塞
   - 🟢 **低优先级**：优化项，可做可不做

### 如何更新本文档

1. **每周结束时**：
   - 更新已完成 Backlog 项的状态（待办 → 进行中 → 已完成/已取消）
   - 记录实际用时（与估算对比）
   - 添加新的 Backlog 项（下一周的工作）

2. **遇到新问题时**：
   - 如果新问题是独立的 Backlog 项，添加到对应周
   - 如果新问题与现有 Backlog 项相关，更新现有项的描述

3. **决策点**：
   - 在 Week 1、2、3 结束时，更新决策检查点的实际决策
   - 如果决策与预期不同，记录原因

### 状态标记

每个 Backlog 项应该有一个状态标记：

| 状态   | 标记 | 含义                 |
| ------ | ---- | -------------------- |
| 待办   | ⬜   | 尚未开始             |
| 进行中 | 🔄   | 正在进行             |
| 已完成 | ✅   | 已完成并通过验证     |
| 已取消 | ❌   | 已取消（不再做）     |
| 阻塞   | 🚫   | 被阻塞（等待其他项） |

---

## 当前状态

**最后更新**: 2026-04-04

### Week 1 状态

| Backlog 项              | 状态    | 备注     |
| ----------------------- | ------- | -------- |
| BL-W1-001: 依赖关系审计 | ⬜ 待办 | 等待开始 |
| BL-W1-002: 代码结构审计 | ⬜ 待办 | 等待开始 |
| BL-W1-003: 测试状态审计 | ⬜ 待办 | 等待开始 |
| BL-W1-004: 功能实现审计 | ⬜ 待办 | 等待开始 |

### Week 2+ 状态

> **注意**: Week 2、3、4 的 Backlog 项将在 Week 1 审计完成后制定。

---

**下一步行动**: 开始 Week 1，执行 BL-W1-001 依赖关系审计。
