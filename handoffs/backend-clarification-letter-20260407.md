# 致后端团队：代码分析 v1.4 技术细节澄清函

**日期**: 2026-04-07  
**发件人**: OpenCode Memory Plugin (插件端) 团队  
**主题**: 代码分析 v1.4 实施细节确认

---

## 1. 感谢与确认

感谢贵团队详细的确认函（`backend-reply-letter-20260407.md`）。我们对以下事项达成一致：

✅ **独立 calls 表方案** - 支持更灵活的查询和扩展  
✅ **嵌套上传 + 独立接口** - 双方案满足不同场景  
✅ **规范化 Git URL 格式** - 统一 project_id 标准  
✅ **2 周 Phase 1 周期** - 2026-04-21 完成 BL-CA-18

现就 5 个技术细节请求进一步澄清，以确保双方实现无缝对接。

---

## 2. 待澄清的技术细节

### 问题 1: `calls` 数组中的 `target` 解析机制

**场景**: 插件端上传的 `calls` 数组包含函数名，后端需要解析为 `memory_id`。

```yaml
metadata.code_analysis.calls: [{ "target": "funcB", "line": 42, "column": 10 }]
```

**疑问**: 后端如何将 `"funcB"` 解析为对应的 `memory_id`？

**建议方案**:

- 后端通过 `(project_id, function_name)` 三元组查找（需建立索引）
- 插件端同时提供 `file_path` 和 `target` 以支持跨文件调用

**请确认**: 是否采用此方案？是否需要插件端提供额外字段？

---

### 问题 2: 调用关系双向查询的实现

**确认**: 两个 API 底层都查询 `calls` 表：

```sql
-- references: 谁调用了我
SELECT * FROM calls WHERE OUT = $memory_id

-- dependencies: 我调用了谁
SELECT * FROM calls WHERE IN = $memory_id
```

**疑问**:

1. 如果调用关系尚未建立（如被调用函数未分析），返回空还是错误？
2. 是否支持递归查询（调用链）？

**请确认**: 第一版是否仅支持单层查询？递归查询是否规划在 Phase 3+？

---

### 问题 3: 代码地图 `file_tree` 数据来源

**场景**: `GET /projects/{id}/map` 返回项目文件树。

**疑问**: `file_tree` 数据来源是？

| 方案                | 优点           | 缺点                 |
| ------------------- | -------------- | -------------------- |
| A. 实时扫描文件系统 | 最新状态       | 可能与记忆数据不一致 |
| B. 从已上传记忆聚合 | 与分析数据一致 | 需要所有文件已上传   |

**建议**: 采用方案 B（从记忆聚合），插件端确保项目文件全量上传。

**请确认**: 是否采用方案 B？是否需要插件端提供文件列表接口？

---

### 问题 4: 复杂度字段命名统一

**发现不一致**:

| 来源               | 字段命名                                      |
| ------------------ | --------------------------------------------- |
| 设计文档 v1.4 §6.1 | `min_complexity` / `max_complexity`           |
| 后端回复信         | `code_complexity_min` / `code_complexity_max` |

**建议**: 采用设计文档命名（`min_complexity` / `max_complexity`），原因：

- 与现有 `code_complexity` 字段风格一致
- 更简洁，符合 Meilisearch filter 惯例

**请确认**: 是否统一为 `min_complexity` / `max_complexity`？

---

### 问题 5: 每周同步的具体安排

**确认**: 建议每周五下午同步。

**疑问**:

1. 具体时间（如周五 16:00-16:30）？
2. 形式（视频会议 / 异步文档更新）？
3. 同步内容（进度更新 / 阻塞问题 / 技术决策）？

**建议**:

- 时间：周五 16:00-16:30（30分钟）
- 形式：异步文档更新为主，阻塞问题即时沟通
- 内容：本周完成、下周计划、阻塞问题

**请确认**: 以上安排是否可行？

---

## 3. 插件端本周交付物

为支持后端 Phase 1 开发，插件端将提供：

| 交付物                       | 格式            | 用途               |
| ---------------------------- | --------------- | ------------------ |
| 示例 CodeAnalysisResult JSON | JSON 文件       | 后端 API 测试数据  |
| CallSymbol 提取实现 PR       | GitHub PR       | 供后端了解数据结构 |
| project_id 规范化工具函数    | JavaScript 代码 | 双方实现参考       |

预计交付时间：2026-04-08（周二）

---

## 4. 下一步行动

### 插件端（本周）:

- [ ] 提供示例 JSON 和工具函数
- [ ] 确认上述 5 个技术细节
- [ ] 启动 BL-CA-12 (CallSymbol 提取) 实现

### 后端（本周）:

- [ ] 回复本澄清函（确认/调整 5 个问题）
- [ ] 创建 BL-CA-18 实施分支
- [ ] 准备联调环境

### 双方共同:

- [ ] 确定每周同步时间（建议周五 16:00）
- [ ] 建立共享文档（API 变更日志）

---

## 5. 联系方式

- **插件端负责人**: OpenCode Memory Plugin Team
- **技术对接**: 通过 GitHub Issue 或邮件
- **紧急联系**: 在 BACKLOG.md 中标注阻塞问题

---

期待贵团队的回复，让我们共同推进 v1.4 的顺利实施！

---

_文档版本: v1.0_  
_日期: 2026-04-07_  
_状态: 待后端确认_
